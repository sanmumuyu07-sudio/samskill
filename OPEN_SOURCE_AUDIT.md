# Samskill 1.0.0-beta.3 开源发布审核

审核日期：2026-08-13。

## 结论

当前版本已经解决自动化可检查的 P0 问题，可以作为 GitHub 公开 Beta 进入陌生用户测试。

它还不能被描述为“已被市场验证的稳定版”。

原因不是结构缺失，而是尚未完成三位陌生用户安装与三个真实项目的完整前向验证。

## 本轮实际修复

### 0. 参考内容双模式

`sam-reconstruct` 不再把“洗稿”统一理解为从零原创。

它先判断：

- 参考型改写：保留原稿问题、判断、重点、功能结构和开头机制。
- 独立原创重构：建立新问题、新论证和独立贡献。

权利状态决定保留尺度，作者身份、案例、数据和承诺必须真实。

### 1. 第一次使用入口

18 个 Skill 均已具备：

- 独立任务说明。
- 最低输入。
- 可提高准确度的材料。
- 材料不足时的降级规则。
- 用户最终会得到什么。
- 可以直接复制的首次使用提示。

### 2. 用户交付

18 个 Skill 均有独立用户版输出模板。

对标与人群采用高颗粒度全景模板。

其他 Skill 至少包含结论、证据、主问题、行动、验收与待确认边界。

机器状态和跨 Skill 交接不再默认出现在用户结果中。

### 3. 开源独立性

- 不依赖外部私有 Skill 才能运行。
- 不含个人电脑绝对路径。
- 删除不参与运行的作者研究全文。
- 删除个人项目内部验收对话。
- 保留公共理论来源、叁木吸收方式和不继承边界。

### 4. 安装与回退

- 安装前支持 `--dry-run`。
- 同名 Skill 冲突时默认停止。
- 显式 `--force` 时先备份再替换。
- 安装后生成版本、文件哈希和安装记录。
- 卸载默认只预览。
- 正式卸载采用可恢复移动。
- 安装后文件发生变化时拒绝直接卸载。

## 自动测试结果

| 检查 | 结果 |
|---|---|
| 根级发布校验 | 通过 |
| 18 个 Skill 独立包校验 | 18／18 通过 |
| SKILL 与 openai YAML 解析 | 36／36 通过 |
| 输入卡 | 18／18 |
| 用户输出模板 | 18／18 |
| 输出质量测试 | 每个 Skill 5—21 个，均含明确断言 |
| 正反触发测试 | 每个 Skill 至少 10／10 |
| 绝对路径扫描 | 0 |
| 用户模板机器状态泄漏 | 0 |
| 安装、重复安装、校验、卸载 | 通过 |
| 冲突拒绝与备份 | 通过 |
| 修改后卸载保护 | 通过 |

## 五维自检

### 逻辑自洽性：通过

总入口只负责识别当前任务。

专项 Skill 负责判断与交付。

用户结果、机器状态和跨 Skill 交接已经分层。

### 受众匹配度：通过候选

新用户不必先理解 18 个 Skill。

既可以从总入口进入，也可以复制专项输入卡。

仍需陌生用户测试“最低输入”是否真的容易填写。

### 认知深度：通过

运行包保留市场、社会心理、传播、内容、产品、交付和实验判断。

没有为了开源体积把方法压成几句提示词。

### 商业逻辑：通过候选

内容不是终点。

系统能够继续连接产品、关系、线索、成交、交付、复购和数字资产。

真实业务结果仍需三个项目测试。

### 差异化强度：成立但待验证

差异不在 Skill 数量。

差异在于把商业判断、人群定位、内容生成、自然流运营、后端承接和资产回填放进同一条可中断、可交接、可复盘的流程。

这条差异已经进入产品结构，但还需要用户结果证明它比单点工具更有价值。

## 人工发布门禁

正式公开采用 MIT 许可证。

版权署名为 `Samskill contributors`。

普通反馈进入 GitHub Issue。

隐私与安全问题优先使用 GitHub 私密漏洞报告。

运行案例只保留脱敏后的业务类型、机制、结果层级与失效边界，不公开客户身份和私有项目文件。
